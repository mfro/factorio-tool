{
  "name": "factorio-tool-test",
  "version": "0.1.0",
  "private": true,
  "main": "main.js",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --outfile=main.js",
    "watch": "esbuild src/main.ts --bundle --platform=node --outfile=main.js --watch",
    "typecheck": "tsc --noEmit",
    "typewatch": "tsc --noEmit --watch"
  },
  "devDependencies": {
    "@types/node": "^14.0.27",
    "esbuild": "^0.12.15",
    "typescript": "^3.9.7"
  }
}
